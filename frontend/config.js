/**
 * MailForge — Frontend Configuration
 * ─────────────────────────────────────────────────────────────
 * AFTER deploying your backend to Render, update the URL below.
 *
 * Step 1: Deploy backend to Render (follow README)
 * Step 2: Copy your Render service URL (e.g. https://mailforge-api.onrender.com)
 * Step 3: Replace the placeholder below with your actual URL
 * Step 4: Redeploy frontend to Vercel (git push — auto-deploys)
 * ─────────────────────────────────────────────────────────────
 */

window.MAILFORGE_CONFIG = {
  // Replace this with your Render backend URL after deployment.
  // Leave unchanged for local development — localhost is used automatically.
  API_URL: "https://mailforge-jble.onrender.com"
};
